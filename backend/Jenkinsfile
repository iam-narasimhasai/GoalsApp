pipeline {
    agent any
    tools {
        nodejs 'nodejs-23-6' 
    }
    environment {
        MONGO_URL = "${params.MONGO_URL}" 
    }
    stages {
         // stage('Git Checkout') {
        //     steps {
        //         git branch: 'pre-prod', url: 'https://github.com/iam-narasimhasai/GoalsApp'
        //     }
        // } this step can be avoided as we directly used Pipelinescript from SCM
        
        stage('Installing Dependencies') {
            steps {
                sh '''
                    cd backend
                    npm install --no-audit
                '''
            }
        }

        stage('OWASP Dependency Check') {
            steps {
                
                dependencyCheck additionalArguments: '--scan backend/ --out ./owasp-report --prettyPrint', odcInstallation: 'OWASP-DepCheck-12'
                
            }
        }

        stage('NPM Dependency Audit') {
            steps {
                sh '''
                    cd backend
                    npm audit --audit-level=critical
                '''
            }
        }

        stage('Run Unit Testing') {
            steps {
                sh '''
                    cd backend
                    npm run test
                '''
            }
        }

        stage('Code Coverage') {
            steps {
                sh '''
                    cd backend
                    npm run coverage
                '''
                
            }
        }
    }
}
